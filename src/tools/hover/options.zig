pub const SymbolsOptions = struct {
    show_public: bool = true,
    show_private: bool = false,
    show_imports: bool = true,
    show_locations: bool = false,
    api_only: bool = false,
    minimal: bool = false,
};

